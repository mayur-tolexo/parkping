version: "3.9"

services:
  api:
    build: .
    container_name: parkping-api-dev
    restart: unless-stopped
    environment:
      APP_ENV: dev
      APP_PORT: 8080
      JWT_SECRET: supersecret
    volumes:
      - .:/app
    ports:
      - "8080:8080"
    command: go run ./cmd/main.go
